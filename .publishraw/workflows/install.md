<!-- PublishRaw package 0.0.1 -->
# Install or repair

Read applicable root/nested instructions and surface specific conflicts. Resolve all paths against the identified repository root. Locate the Git root from the launch directory, inspect staged/unstaged/untracked work and enumerate site candidates. Preserve dirty work, preferences, drafts and other Claude imports. Ask which site to use when ambiguous; a nested launch alone does not select it. Read .publishraw/commands.md for the trusted pinned helper/runner invocation.

For the qualified vite-routerless baseline, bootstrap inspect is read-only. Pass its exact candidate and observedFiles to planConversion with kind install and a newly allocated operation UUID. The runner rechecks the candidate and emits the complete bounded plan, including preserved instruction pointers, profile, local installation record, public modules, narrow Git exclusions and site hooks. No remote is required for this local preparation. Never invent a repository or publishing branch; complete target configuration requires actual evidence.

Save the exact plan under .publishraw/local and import the named planDigest and LocalInstallOperation exports from the verified adapterRunnerEntry module to calculate its digest and validate its operation record. Save a contracts-validated LocalInstallOperation at .publishraw/local/operations/<id>.json with formatVersion 1, id, kind install, siteRoot, repositoryId null, planSha256, the plan baseline and complete change paths. Invoke applyPlan with planPath, planSha256 and operationPath through the same bootstrap envelope. Local installation authorization covers only this bounded preparation. The runner acquires mutation ownership, retains before-images, checks raw baselines and writes install.json last. A saved local record is never publication approval. On interruption retain the exact plan and recovery record; import recoverInstallation from that same verified adapterRunnerEntry module and call await recoverInstallation(repositoryRoot, operationId, new AbortController().signal) to restore only matching owned bytes. These named exports are embedded in the pinned runner; no workspace or global npm import is needed. Unknown or live locks block takeover.

Run the selected profile checks through the helper, then actually compare rendered existing content and public output. Verify drafts/local data remain untracked and absent from loaders, indexes and bundles. Preserve failed checks and later edits; report recovery conflicts. Test fresh current-host discovery from root and nested launch separately; explicit same-session pointer reads do not establish freshness.

Present the exact complete bounded diff and checks for explicit publication approval. When approved, follow .publishraw/rules/publication.md for its isolated-index commit, intent-before-write and configured direct or pull-request path. A dependent item remains TARGET_INSTALLATION_PENDING until the installation bytes are verified on the target branch; it then re-fetches them before its own separately approved publication.

Report collection, host, media and publication states independently as pending, verified, failed, unverified or recovery-conflicted, with their evidence and next actions. Select the first matching overall outcome: unknown attempted remote outcome; no supported collection after discovery; all installation criteria verified; partial if a blocked/failed/unverified component coexists with useful progress; blocked if no progress; awaiting checks/review if approved; awaiting approval if presented; local-ready if prepared; otherwise preparing. Media-blocked text-ready is partial, with publication still local or awaiting approval.

INSTALLATION_INCOMPLETE: A directly published approved diff verifies only its publication component. Fresh desktop discovery and media admission still require their own actual evidence before claiming the full installation installed. Present the bounded diff and checks for publication approval only after local preparation. Do not claim installed from a local record, build or pointer reads. Existing installation repair/resume and complete installation remain separately gated; do not silently overwrite an existing installation or upgrade its pinned release.
