<!-- PublishRaw package 0.0.1 -->
# Update

Read the current item, selected collection JSON/profile guide and preferences. Select exactly the requested branch below; ambiguity requires user direction. Read .publishraw/rules/composition.md, .publishraw/rules/layout.md and .publishraw/rules/publication.md; for images read .publishraw/rules/media.md.

## update
Retain identity, filename and URL while revising the authoritative local draft.

## rename
Require an explicit identity/URL change; validate collisions and report the existing qualified redirect capability and limitations.

## hide
Require the collection's existing qualified visibility convention across its consumers. Absent capability is VISIBILITY_UNAVAILABLE.

## show
Use that same qualified visibility convention to show the selected item. Absent capability is VISIBILITY_UNAVAILABLE.

WORKFLOW_NOT_IMPLEMENTED: This package provides dispatch instructions only. The deterministic mutation and integrated qualification for this workflow are unavailable. Report this blocker and retain local drafts; resume after an explicit qualified package upgrade. A schema-valid request does not establish implementation or user approval.
